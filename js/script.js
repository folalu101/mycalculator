// using prompt to collect user input for subtraction
let number1 = parseInt(prompt("type1"));
let number2 = parseInt(prompt(type2));

function sub(number1, number2){
    return number1 - number2
}
console.log(sub(number1, number2));